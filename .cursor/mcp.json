{
  "mcpServers": {
    "aws-multi-logs": {
      "type": "stdio",
      "command": "node",
      "args": ["${workspaceFolder}/index-mini.js"],
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
